const{generateSW:generateSW}=require("workbox-build"),workboxConfig=require("./workbox-config");generateSW(workboxConfig).then(({count:e,size:r,warnings:o})=>{o.length>0&&console.warn("Warnings encountered while generating a service worker:",o.join("\n")),console.log(`Generated service worker, which will precache ${e} files, totaling ${r} bytes.`)})["catch"](e=>{console.error("Service worker generation failed:",e),process.exit(1)});